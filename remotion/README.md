# MiMs Kanban Studio — Remotion trailer

30s composition. Hero is Studio kit: type a real-world ask → Allow these tools? → board lands.

Use cases: weekly editorial, hiring desk, product launch.
Copy matches the live Operator.

```bash
cd remotion
npm install
npx remotion studio src/index.jsx
npx remotion render src/index.jsx Trailer out/MiMs-Studio-Kit-Trailer.mp4
```

1920×1080 · 30fps · 900 frames.

This clones the live UI language. If you share the app `src/`, we can swap in the real Operator and board components.
